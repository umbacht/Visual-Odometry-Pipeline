function [S_current, T_WC_current] = processFrame(Image_current, Image_previous, S_previous)
    % 1. Associate keypoints in the current frame to previous landmarks
    % 2. Estimate current camera pose
    % 3. Triangulate new landmarks not previously found





    %% Find keypoints from pointcloud 3D -> 2D
    
    
    tracker = 0; %0: Harris, 1: KLT
    
    %% Harris Tracking
    
    if ~tracker
    
        
        
        % Harris scores
    
        % Select Keypoints
    
        % Descirbe query Keypoints
        % Descirbe database Keypoints
    
        % match descriptors












end



end